# AI_USAGE

This project does not require an LLM to function.

If you later plug in an LLM, it should be used only for:
- generating human-friendly explanations for anomalies
- mapping messy CSV rows to normalized fields

All core calculations and anomaly rules remain deterministic.

